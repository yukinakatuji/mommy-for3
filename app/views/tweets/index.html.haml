- @tweets.each do |tweet|
  = render partial: "tweet", locals: { tweet: tweet }
  -# = paginate(@tweets)


-# = form_for (@tweets) do |form|
-#   = form.text_field :keyword, placeholder: 投稿を検索する, class: search-input
-#   = form.submit 検索, class: search-btn




-# .blog-box
-#   = form_for (@post) do |form|
-#     = form.text_field :title, placeholder: :タイトル, class: :form__main
-#     = form.text_area :content, placeholder: :ブログ本文, class: :form__box
-#     = form.submit 'keep a diary🖋', class: :form__send


-# = form_for(url: tweets_searches_path, local: true, method: :get, class: "search-form")